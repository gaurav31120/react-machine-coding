🔵 HARD #1 — Multi-Select Checkbox List
📌 Problem Description

Create a React application that displays a list of checkboxes and allows the user to select multiple items.

🎯 Functional Requirements

Display 3 checkboxes with labels:

React

Angular

Vue

User can:

Select one or more checkboxes

Unselect any selected checkbox

Below the checkbox list, display:

Selected: React, Vue


(Comma-separated values)

If no checkbox is selected:

Show:

Selected: None

🧠 Constraints (IMPORTANT)

Use array state to store selected items

Do NOT use:

Multiple boolean states

External libraries

Checkbox must be controlled

Updates must be immutable

🧪 Edge Cases (Interviewers check these)

Selecting all items

Unchecking one item

Unchecking all items

Clicking the same checkbox repeatedly