🟡 MEDIUM #8 — Accordion Component (Single Open)
📌 Problem Description

Create a React application that displays an Accordion with 3 sections.

Each section should have:

A title

A content area

🎯 Functional Requirements

Initially:

All sections are closed

When a user clicks on a section title:

That section’s content opens

Any previously opened section closes

At any time:

Only ONE section can be open

Clicking an already open section:

Should close it (optional but preferred)

🧠 Constraints (IMPORTANT)

Use only ONE state

Do NOT use:

Multiple booleans (isOpen1, isOpen2, etc.)

CSS-only hiding

Use conditional rendering

Titles and content can be simple text

🖥️ Example UI (conceptual)
▶ Section 1
▶ Section 2
▶ Section 3

(click Section 2)

▼ Section 2
  This is section 2 content