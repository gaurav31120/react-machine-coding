🟢 EASY PROBLEM #6: Show / Hide Password

This is a classic interview question and very quick to implement.

📌 Problem Statement (Interview-style)
Show / Hide Password

Create a React application that allows the user to toggle the visibility of a password field.

✅ Requirements

Display an input field of type password.

Display a button or checkbox labeled Show / Hide.

When the user clicks Show:

The input type should change to text.

When the user clicks Hide:

The input type should change back to password.

The UI should update instantly.

🧠 What this problem tests

Boolean state

Conditional attributes

Controlled inputs

Clean JSX logic

Very common in product-based company interviews.