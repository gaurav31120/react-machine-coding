🟢 EASY #13 — Disable / Enable Input Field
📌 Problem Statement

Create a React component with:

One input field

One button

Button behavior:

Initially → input is enabled

Button text: “Disable”

On click:

Input becomes disabled

Button text changes to “Enable”

Clicking again:

Input becomes enabled again

🎯 Requirements (important)

Use only one boolean state

Use React state — no direct DOM access

Use disabled attribute properly

No CSS tricks for disabling

🧠 Concept Tested

Boolean state toggling

Controlled vs disabled inputs

Conditional button text

🚫 Common Mistakes

❌ Using two states

❌ Using document.getElementById

❌ Disabling button instead of input