🟢 NEXT EASY PROBLEM: Disable Button Based on Input

This is a very common machine-coding question and a natural next step after input handling.

📌 Problem Statement (Interview-style)
Disable Button Based on Input

Create a React application that disables a button when the input field is empty.

Requirements

Display an input field.

Display a button labeled Submit.

When the input field is empty, the button should be disabled.

When the user types something, the button should become enabled.

On clicking Submit, show the entered text below.

🧠 What this problem tests

Controlled components

Conditional props

Boolean expressions

Clean JSX logic

This question appears a lot in product-based interviews.