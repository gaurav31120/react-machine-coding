🟢 EASY #10 — Checkbox Toggle Text
📌 Problem Statement (Machine-coding style)

Create a simple React application that displays a checkbox.
When the checkbox is checked, a text message should appear on the screen.
When the checkbox is unchecked, the text should disappear.

🎯 Exact Requirements (read carefully)

Display a checkbox on the screen

Display the text:
“Accepted”

Initially:

Checkbox is unchecked

Text is not visible

When user:

✅ checks the checkbox → show the text

❌ unchecks the checkbox → hide the text

Use React state (useState)

Do NOT use CSS tricks to hide/show
(use conditional rendering)

🧠 What this problem is testing

Boolean state handling

Controlled checkbox

Conditional rendering

Basic React event handling

This is a very common beginner interview question.

🖥️ Expected UI (simple)
[ ] Accept terms

(when checked)
Accepted