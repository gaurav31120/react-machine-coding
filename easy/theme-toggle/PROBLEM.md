📌 Problem Statement (Interview-style)
Theme Toggle (Light / Dark Mode)

Create a React application that allows the user to toggle between Light mode and Dark mode.

✅ Requirements

Display a button labeled “Switch to Dark Mode” initially.

Clicking the button should:

Change the background color

Change the text color

When Dark mode is active, the button label should change to:

“Switch to Light Mode”

Toggling again should revert the theme back to Light mode.

Use React state to manage the theme.

🧠 What this problem tests

Boolean state handling

Conditional rendering

Conditional CSS classes

Clean UI logic

This question is very common in product-based interviews.