📌 Problem Statement
Toggle Text

Create a simple React application that toggles the visibility of text.

Requirements

Show a button labeled “Show Text” initially.

On button click, display a text message.

When the text is visible, the button label should change to “Hide Text”.

Clicking “Hide Text” should hide the text again.

The UI should update instantly.

🧠 What this problem tests

useState

Boolean state handling

Conditional rendering

Button event handling

This is core machine coding logic.